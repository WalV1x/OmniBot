from discord.ext import commands


class Automods(commands.Cog, name="Automods"):
    def __init__(self, bot) -> None:
        self.bot = bot


async def setup(bot) -> None:
    await bot.add_cog(Automods(bot))
